spring:
    profiles:
    active:
    - dev
---
spring:
  application:
    name: spring_configServer_dev
  profiles: dev
server:
  port: 3306
---
spring:
  application:
    name: spring_configServer_test
  profiles: test
